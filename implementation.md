In this environment, Hardhat rejected passthrough --mode with HH305, so DEPLOY_MODE fallback was added for reliable execution.
For live run, use DEPLOY_MODE=live-gmx plus the required GMX address env vars.
